public List<Integer> positiveIntegers(List<Integer> numbers) {
    //code here
    List<Integer> posInts = new List<Integer>();
    for (Integer i=0; i < numbers.size(); i++){
        if (numbers[i] > 0){
            posInts.add(numbers[i]);
        }
    }
    return posInts;
}